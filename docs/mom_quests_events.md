# Мама ГГ: івенти та квести

**Стан:** усі окремі івенти та квестові гілки мами **прибрані** з коду (чистий старт для нової реалізації).

**Що залишилось (робота мами, без змін за задумом):**

- Розклад і локації роботи в `lib/npcs/mom/mom_npc.dart` (офіс логістики, кава, місто тощо).
- Сцена **кабінету мами** в бізнес-центрі: вхід з логістики, статичні кадри / робочі відео `work_01`–`work_03` (`main_game_quest_and_zone.dart`, `main_game_screen_state_base.dart`, тик часу в `main_game_time_tick_handler.dart`).
- VIP / карта абонемента та пов’язана логіка в `NPCService` (не чіпалась).

**Що видалено (огляд):**

- Івент «помити посуд» / поцілунок (`mom_event_video.dart`, кнопки в `_buildActionPanel`, `main_game_gifting.dart`).
- Оверлей «Вкрити одіялом» і колбек `HomeView.onMomRoomCoverBlanketTap`.
- Камера в **офісі** мами та кнопка «Зняти на відео» / прапор `hasMomOfficeCompromatVideo3` і пункт компромату «Відео мами в офісі» на ноутбуці.
- Міні-квест **college_bully** (кухня після Дена), профіль мами та чит.
- Встановлення міні-камери лише в **кімнаті мами** (залишено для кімнат сестер).

Після додавання нових івентів оновіть цей файл і при потребі поверніть посилання в `README.md`.
